let fruits = ["Apple", "Banana"];
let vegetables = ["Carrot", "Broccoli"];

// sperad opperator is use to shollow copy of the array .for combine the two array.

let combine = [...fruits,...vegetables];
// cancat method is use to combine two array with out modifile the orignal array and creat new array.
let combine2 = fruits.concat(vegetables)
console.log(combine2);

